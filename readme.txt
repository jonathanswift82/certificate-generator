Author: Jonathan Swift

This is a python program generates certificates with header and footer images, parameters need are the spread sheet file name, student names column
-update date.txt and text.txt with correct data
-python certificateManager.py 'SpreadSheetName.xlsx' 'names column name'
-commandline example: python certificateManager.py 'Base.xlsx' 'Nombres y apellidos (como quieres que se escriba en tu constancia)' 

OUTPUT: pdf certificates outputed into directory 'certificates'